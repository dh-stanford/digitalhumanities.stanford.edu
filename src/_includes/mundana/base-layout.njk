<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta content="width=device-width, initial-scale=1, shrink-to-fit=no" name="viewport">
  <title>{{ title + " :: " if title }}Digital Humanities @ Stanford</title>

  {# {% seo %} #}

  <link rel="shortcut icon" type="image/x-icon" href="{{site.baseurl}}/{{site.favicon}}">

  <!-- Font Awesome Icons -->
  <link crossorigin="anonymous" href="https://use.fontawesome.com/releases/v5.3.1/css/all.css"
    integrity="sha384-mzrmE5qonljUremFsqc01SB46JvROS7bZs3IO2EmfFsd15uHvIt+Y8vEf7N7fWAU" rel="stylesheet">

  <!-- Google Fonts-->
  <link href="https://fonts.googleapis.com/css?family=Lora:400,400i,700" rel="stylesheet">

  <!-- Bootstrap Modified -->
  <link rel="stylesheet" href="{{ '/assets/css/main.css' | url }}">

  <!-- Theme Stylesheet -->
  <link rel="stylesheet" href="{{ '/assets/css/theme.css' | url }}">

  <style>
    /* add margin if scroll bar not visible
      Note: leaving space for a scroll bar can result in obviously uneven margins
      on smaller screens when there is no scroll bar.  This is potentially an issue
      on environments without overlay scrollbars and very narrow viewports, but 
      such environments are very uncommon and in my contrived examples behaviour
      is entirely acceptable imo. */
    body {
      margin-left: calc(100vw - 100%);
      margin-right: 0;
      overflow-x: hidden;
    }
    .navbar { left: calc(100vw - 100%); }
  </style>

  {# Social preview metadata (Open Graph + Twitter) #}
  {% set socialTitle = title or 'Digital Humanities @ Stanford' %}
  {% set socialDescription = description or '' %}
  {% set socialType = 'website' if page.url == '/' else 'article' %}
  {% set socialUrl = page.url | url %}

  {% set socialImageUrl = null %}
  {% if post_images and (post_images | length) %}
    {% set socialImageUrl = (post_images[0] | url) %}
  {% elseif site and site.defaultImage %}
    {% set socialImageUrl = site.defaultImage %}
  {% endif %}

  {# Prefer absolute URLs for social cards #}
  {% set absolutePageUrl = (socialUrl | absoluteUrl(site.url)) if site and site.url else socialUrl %}
  {% set absoluteImageUrl = (socialImageUrl | absoluteUrl(site.url)) if socialImageUrl and site and site.url else socialImageUrl %}

  <link rel="canonical" href="{{ absolutePageUrl }}">
  <meta name="description" content="{{ socialDescription }}" />

  <meta property="og:title" content="{{ socialTitle }}" />
  <meta property="og:type" content="{{ socialType }}" />
  <meta property="og:url" content="{{ absolutePageUrl }}" />
  <meta property="og:site_name" content="{{ site.name or 'Digital Humanities @ Stanford' }}" />
  <meta property="og:description" content="{{ socialDescription }}" />
  {% if absoluteImageUrl %}
    <meta property="og:image" content="{{ absoluteImageUrl }}" />
    <meta property="og:image:alt" content="{{ socialTitle }}" />
  {% endif %}

  <meta name="twitter:card" content="{{ 'summary_large_image' if absoluteImageUrl else 'summary' }}" />
  <meta name="twitter:title" content="{{ socialTitle }}" />
  <meta name="twitter:description" content="{{ socialDescription }}" />
  {% if site and site.twitterHandle %}
    <meta name="twitter:site" content="{{ site.twitterHandle }}" />
  {% endif %}
  {% if absoluteImageUrl %}
    <meta name="twitter:image" content="{{ absoluteImageUrl }}" />
  {% endif %}


  <style>
    /* Visually hidden utility for accessible labels */
    .sr-only {
      position: absolute;
      width: 1px;
      height: 1px;
      padding: 0;
      margin: -1px;
      overflow: hidden;
      clip: rect(0, 0, 0, 0);
      border: 0;
      white-space: nowrap;
    }
    /* Skip link */
    .skip-link {
      position: absolute;
      top: -40px;
      left: 0;
      background: #000;
      color: #fff;
      padding: 8px 12px;
      z-index: 10000;
    }
    .skip-link:focus {
      top: 0;
    }
    /* Improve focus visibility */
    a:focus-visible, button:focus-visible, input:focus-visible, [tabindex]:focus-visible {
      outline: 3px solid #212529;
      outline-offset: 2px;
    }
  </style>

</head>

<body class=" {% if page.url==" /" %} homefirstpage {% endif %}">

  <a class="skip-link" href="#main-content">Skip to main content</a>

  <!-- Navbar -->
  <nav class="topnav navbar navbar-expand-lg navbar-light bg-white fixed-top" id="MagicMenu" role="navigation" aria-label="Primary">
    <div class="container">
      <a class="navbar-brand" href="{{ '/' | url }}">
        <strong>{{ "Digital Humanities @ Stanford" }}</strong>
      </a>
      <button aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation"
        class="navbar-toggler collapsed" data-target="#navbarColor02" data-toggle="collapse" type="button">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="navbar-collapse collapse" id="navbarColor02" style="">
        <ul class="navbar-nav mr-auto d-flex align-items-center">
          {% include "../mundana/menu-header.liquid" %}
        </ul>
        <ul class="navbar-nav ml-auto d-flex align-items-center">
          <li class="nav-item">
            {% include "../mundana/search-lunr.liquid" %}
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Content -->
  <main class="site-content" role="main" id="main-content" tabindex="-1">
    {{ content | safe }}
  </main>

  <script src="{{ '/assets/js/theme.js' | url }}"></script>

    <!-- Footer -->
    <footer class=" bg-white border-top p-3 text-muted small">
      <div class="container">
        <div class="row align-items-center justify-content-between">
          <div>
            <span class="navbar-brand mr-2 mb-0">
              <strong>Digital Humanities @ Stanford</strong>
            </span>
          </div>
          <div>
            <span>Copyright © 2022.</span>

            <a class="text-dark ml-1" href="https://github.com/dh-stanford/dh-stanford.github.io/" target="_blank" rel="noopener noreferrer" aria-label="View site source on GitHub (opens in a new tab)">
              <i class="fab fa-github"></i>
              Site Source
            </a>
          </div>
        </div>
      </div>
    </footer>

  </body>

</html>
